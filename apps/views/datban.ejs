<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sơ đồ bàn</title>
</head>

<body>
    <div class="container">
        <div class="content">
            <% for(let i=0; i < data.length; i++) { %>

                <a class="table" href="/orderTable/choose?name=<%= data[i].NameTable %>">
                    <div class="title">
                        <%= data[i].NameTable %>
                    </div>
                    <div class="descripstion">
                        <%= data[i].Position %>
                    </div>

                </a>


                <% } %>
        </div>
    </div>
    <footer>
        <div class="left">
            <button>Đặt bàn</button>
            <button>Thông tin đặt</button>
        </div>
        <div class="right">
            <button>In hóa đơn</button>
        </div>
    </footer>

    <style>
        body {
            margin: 0;
        }

        .container {
            height: 85vh;
            background-color: #A8A8A8;
            overflow-y: auto;
        }

        .content {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            padding: 10px;
            margin: 0 15rem;
        }

        .table {
            margin: 10px;
            height: 10rem;
            width: 8rem;
            background-color: yellow;
            border: 1px solid #FE6244;
            border-radius: 2px;
            cursor: pointer;
            text-decoration: none;
        }

        .title {
            font-weight: 700;
            padding: 5px;
            border-bottom: 1px solid #FE6244;
            text-align: center;

        }

        .descripstion {
            text-align: center;
        }

        footer {
            background-color: #F4B183;
            height: 15vh;
            display: flex;
            justify-content: space-between;
        }

        .left {
            display: flex;
        }

        button {
            height: 100%;
            width: 10rem;
            margin: 0 1rem;
            cursor: pointer;
        }
    </style>
</body>

</html>